import Foundation

/// Where the app lives online — one place to update when the listing ships.
enum AppLinks {
    /// Empty until the App Store listing exists; share cards omit the URL
    /// line while it's empty, so nothing dead ever prints on a card.
    static let appStoreShortURL = ""
}
