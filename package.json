{
  "name": "beenthere-eas-shim",
  "version": "1.0.0",
  "private": true,
  "description": "Build-infrastructure shim only. Been There is a pure Swift/SwiftUI app with no JavaScript runtime — this file exists solely because EAS Build requires a package.json at the project root. Nothing here ships inside the app. See docs/RELEASE.md (Route B).",
  "dependencies": {
    "expo": "57.0.8"
  }
}
